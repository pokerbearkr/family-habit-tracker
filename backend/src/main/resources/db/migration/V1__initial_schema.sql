-- Initial schema (baseline for existing database)
-- This file documents the existing schema structure

-- Note: If your database already has these tables, you need to baseline Flyway
-- Run this command on your production database:
-- flyway baseline -baselineVersion=1

-- This migration will be skipped if tables already exist
